package overriding;

/*FirefoxDriver does not want to override the click method
 * so we can use click() method from WebDriver class by extends the WebDriver class
 */
public class FirefoxDriver extends WebDriver{

}
